<odoo>
	<data>
		<template id="report_invoice_document_custom" inherit_id="account.report_invoice_document">
		
			<xpath expr="//div[contains(@class, 'page')]//h2" position="replace">
				<h2 t-if="o.move_type == 'out_refund'">
					<span>Factura Rectificativa</span>
				</h2>
				<h2 t-if="o.move_type != 'out_refund'">
					<span>Factura</span>
				</h2>
			</xpath>
            
            <xpath expr="//div[@id='informations']" position="replace">
				<div id="informations" class="row mt-4 mb-4">
                    <div class="col-auto col-3 mw-100 mb-2" t-if="o.invoice_date" name="invoice_date">
                        <strong>Fecha factura:</strong>
                        <p class="m-0" t-field="o.invoice_date"/>
                    </div>
                    <div class="col-auto col-3 mw-100 mb-2" name="reference">
                        <strong>Número Factura:</strong>
                        <p class="m-0" t-field="o.name"/>
                    </div>
                </div>
			</xpath>
			
			
			<xpath expr="//table[@name='invoice_line_table']" position="replace">
				<table class="table table-sm o_main_table" name="invoice_line_table">
                    <thead>
                        <tr>
                            <th name="th_description" class="text-start"><span>Descripción</span></th>                            
                            <th name="th_discount" t-if="display_discount" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>Dto.(%)</span>
                            </th>                            
                            <th name="th_subtotal" class="text-end">
                                <span>Total</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="invoice_tbody">
                        <t t-set="current_subtotal" t-value="0"/>
                        <t t-set="current_total" t-value="0"/>
                        <t t-set="lines" t-value="o.invoice_line_ids.sorted(key=lambda l: (-l.sequence, l.date, l.move_name, -l.id), reverse=True)"/>

                        <t t-foreach="lines" t-as="line">
                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                            <t t-set="current_total" t-value="current_total + line.price_total"/>

                            <tr t-att-class="'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">
                                <t t-if="line.display_type == 'product'" name="account_invoice_line_accountable">
                                    <td name="account_invoice_line_name"><span t-if="line.name" t-field="line.name" t-options="{'widget': 'text'}">Bacon Burger</span></td>
                                    <td name="td_discount" t-if="display_discount" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span class="text-nowrap" t-field="line.discount">0</span>
                                    </td>                                    
                                    <td name="td_subtotal" class="text-end o_price_total">
                                        <span class="text-nowrap" t-field="line.price_subtotal">27.00</span>
                                    </td>
                                </t>
                                <t t-elif="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name" t-options="{'widget': 'text'}">A section title</span>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-elif="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name" t-options="{'widget': 'text'}">A note, whose content usually applies to the section or product above.</span>
                                    </td>
                                </t>
                            </tr>

                            <t t-if="current_section and (line_last or lines[line_index+1].display_type == 'line_section')">
                                <tr class="is-subtotal text-end">
                                    <td colspan="99">
                                        <strong class="mr16">Subtotal</strong>
                                        <span
                                            t-out="current_subtotal"
                                            t-options='{"widget": "monetary", "display_currency": o.currency_id}'
                                        >31.05</span>
                                    </td>
                                </tr>
                            </t>
                        </t>
                    </tbody>
                    
                </table>
			</xpath>
			
			<!-- <xpath expr="//t/t/div/p[1]" position="replace">
                <p t-if="o.team_id.name == 'Mantenimientos'">
					<strong>Esta factura cubre el siguiente periodo:</strong>
					<span t-field="o.invoice_date" t-options='{"format": "MM/yyyy"}'/>
                    <br/>
				</p>
            </xpath> -->
            
            <!-- <xpath expr="//div[@t-if='not is_html_empty(o.narration)']" position="after"> -->
            <xpath expr="//div[@id='payment_term']" position="after">                
                <div>
                    <p t-if="o.team_id.name == 'ERP'">
                        <strong>Notas:</strong>
                        <br/>
                        <span t-field="o.narration"/>
                        <br/>
                    </p>
                    <p t-if="o.team_id.name == 'Software a medida'">
                        <strong>Notas:</strong>
                        <br/>
                        <span t-field="o.narration"/>
                        <br/>
                    </p>
                    <p t-if="o.team_id.name == 'SERVICIOS'">
                        <strong>Notas:</strong>
                        <br/>
                        <span t-field="o.narration"/>
                        <br/>
                    </p>
                </div>
            </xpath>

            <xpath expr="//div[@t-if='not is_html_empty(o.narration)']" position="replace"/>
                
                    
            <xpath expr="//div[@id='payment_term']" position="after">
				<p t-if="o.team_id.name == 'Mantenimientos'">
					<strong>Fecha vencimiento:</strong>
					<span t-field="o.invoice_date_due"/>
				</p>
			</xpath>
            
            <xpath expr="//p[@t-if='not is_html_empty(o.fiscal_position_id.note)']" position="replace">
                <p t-if="o.team_id.name == 'Mantenimientos'">
					<strong>Forma de pago:</strong>
					<span t-field="o.invoice_payment_term_id"/>
				</p>
            </xpath>

            <xpath expr="//div[p[@name='payment_communication']]" position="replace"/>
			
        </template>
        <template id="document_tax_totals_template_inherit" inherit_id="account.document_tax_totals_template">
            <xpath expr="//tr[hasclass('border-black','o_subtotal')]/td" position="replace">
                <td><strong>Importe libre de impuestos</strong></td>
            </xpath>
            
        </template>
	</data>
</odoo>
        